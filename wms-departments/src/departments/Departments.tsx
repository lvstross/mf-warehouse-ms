import React from 'react';

export interface Props {}

const Departments = ({}: Props) => {
    return (
        <div>
            <h1>Departments</h1>
        </div>
    );
};

export default Departments;